// Those the keys to accessing the chrome storage, if they will change someday it will be more convenient to change them only here.

const urlsToDecodeKey = 'urlsToDecode';
const decodedUrlsKey = 'decodedUrls';

export { urlsToDecodeKey, decodedUrlsKey };